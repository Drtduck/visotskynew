---
const links = [
  { text: "Главная", href: "/" },
  { text: "Обо мне", href: "/about" },
  { text: "Блог", href: "/blog" },
];

const socials = [
  { name: "Telegram", href: "https://t.me/asvpsy", icon: "telegram" },
  { name: "Twitter", href: "#", icon: "twitter" },
  { name: "Medium", href: "#", icon: "medium" },
];
---

<header class="fixed w-full bg-[#13151a]/90 backdrop-blur-sm z-50">
  <div class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
    <a href="/" class="text-xl font-bold">Андрей Высоцкий</a>
    
    <nav class="flex gap-8">
      <ul class="flex gap-6">
        {links.map(link => (
          <li>
            <a href={link.href} class="hover:text-purple-400 transition-colors">
              {link.text}
            </a>
          </li>
        ))}
      </ul>
      
      <ul class="flex gap-4">
        {socials.map(social => (
          <li>
            <a href={social.href} target="_blank" rel="noopener noreferrer" 
               class="hover:text-purple-400 transition-colors">
              <span class="sr-only">{social.name}</span>
              <i class={`fab fa-${social.icon}`}></i>
            </a>
          </li>
        ))}
      </ul>
    </nav>
  </div>
</header>